[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { workspace = true }
common = { path = "../common" }
rusqlite = { version = "0.40.1", features = ["bundled", "chrono"] }
serde = { workspace = true }
serde_json = "1.0.151"
